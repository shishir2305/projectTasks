import React from 'react';
import {View} from 'react-native';

const HorizontalNormalLine = () => {
  return (
    <View
      style={{
        marginLeft: 10,
        marginRight: 10,
        borderRadius: 1,
        borderWidth: 0.3,
        borderColor: 'rgba(161,155,183,1)',
        marginVertical: 20,
      }}
    />
  );
};

export default HorizontalNormalLine;
